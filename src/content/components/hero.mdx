---
title: "Hero"
lastUpdate: 2025-07-03
version: 0.9
type: "component"
---

import Notice from "../../components/Notice.astro";
import Hero from "../../components/Hero.astro";
import backgroundImage from "../../assets/images/triangle-345015062.svg";

<section class="prose">

The `Hero` component offers highly flexible, accessible hero sections for landing pages, product sites, and marketing. It supports full, split, and slideshow layouts; accepts image, video, gradient, or animated canvas backgrounds; provides overlay and text color controls; allows custom aspect ratios and content/media ordering; supports parallax, rounded corners, and flush layouts; and is fully slot-based for custom content. ([Full size demo](/demos/hero).)

<Hero
  variant="full"
  height={40}
  media={{ type: "canvas", src: "/_js/triangles.js" }}
  overlayOpacity={0.3}
  overlayColor="--primary-950"
  roundCorners
  textColor="--base-0"
  extraClass="mt-sm3"
>
  <h1>Example Hero</h1>
  <p>This hero uses an animated canvas element as background.</p>
</Hero>

<div class="docs_oversizedTable">

| File Name            | class   | Source      |
| -------------------- | ------- | ----------- |
| `component.hero.css` | `.hero` | [Github][1] |

[1]: https://github.com/minimaldesign/mCSS/blob/main/src/styles/component.hero.css

</div>

#### Full layout (default)

The full layout spans the entire viewport with content (headline, subheadline, CTA) centered or aligned over a background image, video, gradient, or animated canvas. Use the `height` prop to control min-height in viewport units.

#### Split layout

The Split layout features an asymmetric split (e.g., `70/30`, `80/20`, etc.) with content on one side and media on the other. The media section fills the available space with an image or video, cropping as needed. The `ratio` prop defines the grid layout, `contentFirst` controls the DOM order of content and media, and `reversed` visually reverses their order (while the grid ratio remains unchanged).
The media section

#### Slideshow layout

The Slideshow layout features rotating content (such as multiple headlines, CTAs, or background media) with pagination dots. Each slide can have its own media and content.

<Notice
  type="warning"
  title="Multiple Hero components on a single page"
  extraClass="mt-md3"
>
  For `Hero` components that require JavaScript (such as canvas backgrounds or
  slideshows), including more than one Hero may duplicate JavaScript execution.
  Make sure to add a unique `id` to each instance to ensure scripts target the
  correct element.
</Notice>

</section>

## HTML

<section class="prose">

### Custom properties

The following custom properties are available in the [default theme](/docs/themes):

| Property                             | Description                                     |
| ------------------------------------ | ----------------------------------------------- |
| `--hero-radius`                      | Border radius (on/off via `roundCorners` prop). |
| `--hero-content-padding`             | Padding for hero content area.                  |
| `--hero-pagination-dot`              | Pagination dot size.                            |
| `--hero-pagination-dot-color`        | Pagination dot background color.                |
| `--hero-pagination-dot-active-color` | Active pagination dot color.                    |
| `--hero-pagination-opacity`          | Pagination dot default opacity.                 |

</section>

## Astro component

<section class="prose">

### General props

| Prop           | Type    | Default | Description                                                   |
| -------------- | ------- | ------- | ------------------------------------------------------------- |
| `variant`      | string  | `full`  | Layout variant: Full, Split, and Slideshow.                   |
| `media`        | object  | —       | Image, video, gradient, or canvas background.                 |
| `height`       | number  | —       | Height (`vh`) for the Full and Slideshow variants.            |
| `roundCorners` | boolean | `false` | Whether the hero media has rounded corners.                   |
| `textColor`    | string  | —       | Text color (CSS variable or value).                           |
| `extraClass`   | string  | —       | Additional CSS classes (see [helper classes](/docs/helpers)). |

### Split variant Props

| Prop           | Type    | Default | Description                                                                              |
| -------------- | ------- | ------- | ---------------------------------------------------------------------------------------- |
| `ratio`        | string  | `62/38` | Split ratio, e.g. `70/30`, `40/60`, etc.                                                 |
| `contentFirst` | boolean | `false` | If set, content appears before media in the DOM (content on the left on desktop).        |
| `reversed`     | boolean | `false` | Visually reverses the content order set by `contentFirst`. (No change to DOM.)           |
| `flush`        | boolean | `false` | Removes the outer padding from the content section, making the text flush with the edge. |

### Full and slideshow variant props

| Prop                  | Type    | Default      | Description                                |
| --------------------- | ------- | ------------ | ------------------------------------------ |
| `parallax`            | boolean | `false`      | Parallax effect (full variant only).       |
| `height`              | number  | —            | Min-height in vh (0-100) for full variant. |
| `overlayOpacity`      | number  | `0`          | Overlay opacity for full/slideshow.        |
| `overlayColor`        | string  | `--base-950` | Overlay color (CSS var or value).          |
| `roundCorners`        | boolean | `false`      | Hero media has rounded corners?            |
| `textColor`           | string  | —            | Text color (CSS var or value).             |
| `slideshowItems`      | array   | —            | Items for slideshow.                       |
| `slideshowAutoplay`   | boolean | `true`       | Slideshow autoplay.                        |
| `slideshowInterval`   | number  | `5000`       | Slideshow interval (ms).                   |
| `slideshowTransition` | number  | `600`        | Slideshow transition speed (ms).           |

</section>

## Examples

<ul class="docs_examples">
  <li>

    ### Animated Canvas
    <Hero
      variant="full"
      height={50}
      media={{ type: "canvas", src: "/_js/triangles.js" }}
      overlayOpacity={0.3}
      overlayColor="--primary-950"
      roundCorners
      textColor="--base-0"
    >
      <h1>Animated Canvas Hero</h1>
      <p>This hero uses an animated canvas element as background.</p>
      <a class="bt bt-lg bt-outline-white mt-md1" href="/demos/hero">Full Size Examples</a>
    </Hero>

```astro
---
import Hero from "../../components/Hero.astro";
---
<Hero
  variant="full"
  height={50}
  media={{ type: "canvas", src: "/_js/triangles.js" }}
  overlayOpacity={0.3}
  overlayColor="--primary-950"
  roundCorners
  textColor="--base-0"
>
  <h1>Animated Canvas Hero</h1>
  <p>Organic Delaunay triangles animated with primary colors.</p>
  <a class="bt bt-primary">Full Size Demo</a>
</Hero>
```

  </li>
  <li>
    ### Gradient background
    <Hero
      variant="full"
      height={50}
      media={{
        type: "gradient",
        src: "linear-gradient(120deg, #ff0080 0%, #7928ca 50%, #007cf0 100%)",
      }}
      overlayOpacity={0.7}
      roundCorners
      textColor="--base-0"
    >
      <h1>Gradient Background Hero</h1>
      <p>Showcase your brand with a beautiful, vibrant gradient background.</p>
      <a class="bt bt-lg bt-outline-white mt-md1" href="/demos/hero">Full Size Examples</a>
    </Hero>

```astro
---
import Hero from "../../components/Hero.astro";
---
<Hero
  variant="full"
  height={50}
  media={{
    type: "gradient",
    src: "linear-gradient(120deg, #ff0080 0%, #7928ca 50%, #007cf0 100%)",
  }}
  roundCorners
  textColor="--base-0"
>
  <h1>Gradient Background Hero</h1>
  <p>Showcase your brand with a beautiful, vibrant gradient background.</p>
  <a class="bt bt-lg bt-outline-white mt-md1" href="/demos/hero">Full Size Examples</a>
</Hero>
```

  </li>
  <li>
    ### Background image
    <Hero
      variant="full"
      height={50}
      media={{ type: "image", src: backgroundImage, alt: "Background Example" }}
      overlayColor="--primary-950"
      overlayOpacity={0.4}
      roundCorners
      textColor="--base-0"
    >
      <h1>Image Background Hero</h1>
      <p>This hero uses a static image as the background.</p>
      <a class="bt bt-lg bt-outline-white mt-md1" href="/demos/hero">Full Size Examples</a>
    </Hero>

```astro
---
import Hero from "../../components/Hero.astro";
import backgroundImage from "../../assets/images/triangles.svg";
---
<Hero
  variant="full"
  height={50}
  media={{ type: "image", src: backgroundImage, alt: "Background Example" }}
  overlayColor="--primary-950"
  overlayOpacity={0.4}
  roundCorners
  textColor="--base-0"
>
  <h1>Image Background Hero</h1>
  <p>This hero uses a static image as the background.</p>
  <a class="bt bt-lg bt-outline-white mt-md1" href="/demos/hero">Full Size Examples</a>
</Hero>
```

  </li>
  <li>
    ### Default split
    <Hero
      variant="split"
      roundCorners
      media={{ type: "image", src: backgroundImage, alt: "Split Example" }}
    >
      <h1>Split Hero (Content Right)</h1>
      <p>Default split with content on the right and media on the left.</p>
    </Hero>

```astro
---
import Hero from "../../components/Hero.astro";
import backgroundImage from "../../assets/images/triangles.svg";
---
<Hero
  variant="split"
  roundCorners
  media={{ type: "image", src: backgroundImage, alt: "Split Example" }}
>
  <h1>Split Hero (Content Right)</h1>
  <p>Default split with content on the right and media on the left.</p>
</Hero>
```

  </li>
  <li>
    ### Content first split
    <Hero
      variant="split"
      ratio="62/38"
      contentFirst
      flush
      roundCorners
      media={{ type: "image", src: backgroundImage, alt: "Split Example" }}
    >
      <h1>Split Hero (Content First)</h1>
      <p>The `contentFirst` prop places the content first in the DOM (above on mobile and on the left on desktop).</p>
    </Hero>

```astro
---
import Hero from "../../components/Hero.astro";
import backgroundImage from "../../assets/images/triangles.svg";
---
<Hero
  variant="split"
  ratio="62/38"
  contentFirst
  flush
  roundCorners
  media={{ type: "image", src: backgroundImage, alt: "Split Example" }}
>
  <h1>Split Hero (Content First)</h1>
  <p>The `contentFirst` prop places the content first in the DOM (above on mobile and on the left on desktop).</p>
</Hero>
```

  </li>
  <li>
    ### Content first reversed  split
    <Hero
      variant="split"
      ratio="60/40"
      contentFirst={false}
      reversed
      flush
      roundCorners
      media={{ type: "image", src: backgroundImage, alt: "Split Example" }}
      extraClass="prose"
    >
      <h1>Split Hero (Reversed)</h1>
      <p>`contentFirst` is set to `false`, but the content still appears first because of the `reversed` prop. The `flush` prop is used to remove left padding.</p>
    </Hero>

```astro
---
import Hero from "../../components/Hero.astro";
import backgroundImage from "../../assets/images/triangles.svg";
---
<Hero
  variant="split"
  ratio="60/40"
  contentFirst={false}
  reversed
  flush
  roundCorners
  media={{ type: "image", src: backgroundImage, alt: "Split Example" }}
  extraClass="prose"
>
  <h1>Split Hero (Reversed)</h1>
  <p>`contentFirst` is set to `false`, but the content still appears first because of the `reversed` prop. The `flush` prop is used to remove left padding.</p>
</Hero>
```

  </li>
</ul>
