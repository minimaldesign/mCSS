---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'li'> {
  title?: string;
  titleClass?: string;
  iconClass?: string;
  'data-testid'?: string;
}

// prettier-ignore
const {
  title,
  titleClass,
  iconClass,
  class: className,
  'data-testid': testId = 'featureItem',
  ...rest
} = Astro.props as Props;
---

<!-- prettier-ignore -->
<li
  class:list={['featureItem', className]}
  data-testid={testId}
  {...rest}
>
  {
    Astro.slots.has('icon') && (
      <div class:list={['featureItem_icon', iconClass]} aria-hidden="true">
        <slot name="icon" />
      </div>
    )
  }
  {title && <h3 class:list={['featureItem_title', titleClass]}>{title}</h3>}
  <div class="featureItem_text">
    <slot />
  </div>
</li>
